Import('programs_env')

toplevel_srcs = map(File, [
    'area.cc',
    'area.h',
    'sidebar.h',
    'tabs_notebook.cc',
    'tabs_notebook.h'
    ])

srcs = toplevel_srcs

# Distribute the build files.
programs_env.Dist('Makefile.am', 'SConscript')

Return('srcs')
